# Two Web UI are created using Streamlit framework. The user can upload a audio file into the web UI and it converts the audio to text. The following are the source code and its dependencies

* openai-whisper - OpenAI’s Whisper speech-to-text library
* setuptools-rust
* Whisper requires a tool called FFmpeg